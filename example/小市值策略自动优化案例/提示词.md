针对文件 `small_cap_strategy.py` `小市值策略 — 降低回撤、提升收益.md` 中实现的小盘股策略，系统性地提出10项具体优化建议。每项建议需包含明确的优化方向、技术实现路径及预期改进目标。对每项优化建议进行独立实施，完成后使用以下指令进行策略回测：
```
python main.py --mode backtest --strategy small_cap --period 1d --pool 中证1000 --start 2020-04-28 --end 2026-04-28 --debug
```
以"夏普比率"作为核心衡量指标，记录每次优化前后的策略表现数据，当前在未修改的情况下，夏普比率为1.94。将每次优化的完整代码实现及对应的回测结果（包含关键指标对比、性能数据及可视化图表）保存至本地指定目录，建立清晰的版本管理机制以便后续对比分析。对于优化效果显著（行比率提升≥5%）的方案予以保留并整合，对于未达预期或导致性能下降的优化方案则予以放弃，并在文档中记录具体原因及经验总结。